<ivysettings>

  <!-- ====================================================================== -->

  <settings defaultResolver="chain-resolver" />
  
  <!-- ====================================================================== -->

   <resolvers> 
	  <!--
	<url name="shared" m2compatible="true">
      <ivy      pattern="https://repo.eng.pivotal.io/artifactory/gpdb-ext-release-local/[organization]/[module]/[revision]/ivys/ivy-[revision].xml" />
      <artifact pattern="https://repo.eng.pivotal.io/artifactory/gpdb-ext-release-local/[organization]/[module]/[revision]/[type]s/[module]-[revision].[ext]" />
  </url>
  -->
	<chain name="chain-resolver" returnFirst="true" >
	<!--		
	<url name="shared" m2compatible="true">
        <artifact
            pattern="http://repo1.maven.org/maven2/[organization]/[module]/[revision]/[module]-[revision].[ext]"/>
	</url>
	-->
    <url name="shared" m2compatible="true">
        <artifact
            pattern="http://repo.spring.io/libs-release-remote/[organization]/[module]/[revision]/[module]-[revision].[ext]"/>
    </url>

    <filesystem name="local">
      <ivy      pattern="/opt/ivy/modules/[organisation]/[module]/ivy-[revision].xml" />
      <artifact pattern="/opt/ivy/modules/[organisation]/[module]/[type]s/[module]-[revision].[ext]" />
    </filesystem>

	</chain>

	</resolvers>
  <include url="${ivy.default.settings.dir}/ivysettings-public.xml"/>
  <include url="${ivy.default.settings.dir}/ivysettings-main-chain.xml"/>
  <include url="${ivy.default.settings.dir}/ivysettings-default-chain.xml"/>

  <!-- ====================================================================== -->

  <triggers>
    <ant-call target="download-untar-trigger"
              prefix="dep"
              event="post-download-artifact"
              filter="type=targz AND status=successful"/>
  </triggers>

  <!-- ====================================================================== -->

</ivysettings>
